/**
 * E2E: caching & background polling — TODO scaffold (NOT YET IMPLEMENTED).
 * Pending specs only. See ../README.md.
 */
declare const test: { todo(name: string): void };

test.todo("first paint comes from the on-disk cache before the network resolves");
test.todo("data refetches every 60s and on window focus");
test.todo("viewed-file state persists across an app restart");

export {};
