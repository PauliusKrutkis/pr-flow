/**
 * E2E: PR review screen — TODO scaffold (NOT YET IMPLEMENTED).
 * Pending specs only. See ../README.md.
 */
declare const test: { todo(name: string): void };

test.todo("opening a PR shows the file sidebar and the diff");
test.todo("Esc returns from the review screen to the inbox");
test.todo("n / p switch to the next / previous file");
test.todo("j / k scroll the diff");
test.todo("v toggles the file's viewed state (checkmark + header count) and advances");
test.todo("]c / [c navigate between comment threads");
test.todo("'o' opens the PR files on GitHub (opener mocked)");
test.todo("'i' toggles the info panel");
test.todo("diff renders add/del/context rows with syntax highlighting");
test.todo("hunks are collapsible");
test.todo("a binary / no-patch file shows the explanatory note");

export {};
