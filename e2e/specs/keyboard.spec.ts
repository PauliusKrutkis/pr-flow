/**
 * E2E: keyboard, command palette & help — TODO scaffold (NOT YET IMPLEMENTED).
 * Pending specs only. See ../README.md.
 */
declare const test: { todo(name: string): void };

test.todo("Cmd/Ctrl+K opens the command palette; typing filters entries");
test.todo("Enter runs the highlighted command; arrows move the selection");
test.todo("the palette lists 'jump to PR' entries and navigates to one");
test.todo("an open palette suppresses the underlying screen's single-key shortcuts");
test.todo("'?' opens the help overlay and Esc closes it");
test.todo("two-key sequences (]c / [c) and mod+ combos resolve correctly");
test.todo("bindings are ignored while typing in inputs / textareas");

export {};
