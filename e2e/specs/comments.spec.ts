/**
 * E2E: comments — TODO scaffold (NOT YET IMPLEMENTED).
 * Pending specs only. See ../README.md.
 */
declare const test: { todo(name: string): void };

test.todo("existing review comments anchor to the correct line and side");
test.todo("the inline '+' opens the composer beneath the row");
test.todo("submitting an inline comment posts it and it appears after refetch");
test.todo("replying to a thread posts with the correct inReplyTo id");
test.todo("a PR-level comment can be posted from the info panel composer");
test.todo("Cmd/Ctrl+Enter submits; Esc cancels; an empty submit is ignored");

export {};
